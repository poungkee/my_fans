# FANS 프로젝트 발표 대본

---

## 🎤 슬라이드 1: 프로젝트 소개

안녕하세요. FANS 뉴스 큐레이션 플랫폼을 발표하겠습니다.

FANS는 "Fact-checked AI News Service"의 약자로, AI 기반 뉴스 큐레이션 및 편향성 분석 플랫폼입니다.

**(잠깐 멈춤)**

최근 가짜 뉴스와 편향된 보도가 사회적 문제가 되고 있습니다. 저희 팀은 이러한 문제를 해결하고자 AI를 활용한 뉴스 플랫폼을 개발했습니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 2: 핵심 기능

FANS의 핵심 기능은 크게 네 가지입니다.

**첫째**, 3분마다 자동으로 뉴스를 수집합니다. 사람이 직접 수집하지 않아도 실시간으로 최신 뉴스가 업데이트됩니다.

**둘째**, AI가 기사를 100자로 요약합니다. 바쁜 현대인들이 빠르게 핵심 내용을 파악할 수 있습니다.

**셋째**, 편향성 분석 기능입니다. 기사의 감성이 긍정인지 부정인지, 그리고 정치적 성향이 어떤지 AI가 자동으로 분석합니다.

**넷째**, 8개 카테고리로 자동 분류됩니다. 정치, 경제, 사회, 세계, IT, 생활문화, 스포츠, 연예 총 8개 카테고리로 정확하게 분류됩니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 3: 기술 스택

이제 사용한 기술 스택을 설명드리겠습니다.

**프론트엔드**는 React 18을 사용했고, Axios로 API 통신을 합니다.

**백엔드**는 Node.js 기반이며, TypeScript 5.9 버전을 사용했습니다. TypeScript를 선택한 이유는 타입 안전성을 보장받기 위해서입니다. Express.js 프레임워크를 사용했고, TypeORM으로 데이터베이스를 관리합니다. 인증은 JWT 토큰과 bcrypt 암호화를 사용합니다.

**데이터베이스**는 PostgreSQL 15 버전을 메인 데이터베이스로 사용하고, Redis 7을 캐시와 세션 저장소로 사용합니다.

**AI 부분**이 핵심인데요, Python 3.10과 FastAPI를 사용했습니다. 요약에는 T5 한국어 특화 모델을, 감성 분석에는 KoBERT를, 카테고리 분류에는 Random Forest 알고리즘을 사용했습니다.

소셜 로그인은 카카오와 네이버 OAuth 2.0을 연동했습니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 4: 시스템 아키텍처

시스템 구조를 간단히 설명드리면,

사용자가 React로 만든 프론트엔드에 접속하면, 3001번 포트로 접근합니다.

프론트엔드는 3000번 포트의 Node.js API 서버와 통신하고,

API 서버는 PostgreSQL 데이터베이스에 데이터를 저장하고 조회합니다.

그리고 API 서버 아래에는 세 개의 AI 서비스가 있습니다.

8000번 포트의 요약 AI, 8002번 포트의 편향 분석 AI, 5000번 포트의 분류 AI가 각각 독립적으로 동작합니다.

이렇게 마이크로서비스 아키텍처로 구성해서 각 서비스를 독립적으로 확장할 수 있습니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 5: 뉴스 크롤링 시스템 - 수집 규모

이제 가장 중요한 크롤링 시스템을 설명드리겠습니다.

**(표를 가리키며)**

크롤링 주기는 **3분마다** 자동으로 실행됩니다.

8개 카테고리에서 각각 5개씩, 즉 한 번에 **40개의 기사**를 수집합니다.

계산해보면, 시간당 20번 실행되니까 **시간당 800개**,

하루로 계산하면 **19,200개의 기사**를 자동으로 수집합니다.

**(강조)**

이 모든 과정이 완전히 자동화되어 있어서, 사람이 전혀 개입하지 않아도 24시간 계속 뉴스를 수집합니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 6: 뉴스 크롤링 시스템 - 처리 프로세스

뉴스는 2단계로 처리됩니다.

**1단계는 원본 수집**입니다.

3분마다 Daum News에서 HTML을 파싱해서 기사를 가져옵니다. 그리고 raw_news_articles라는 임시 테이블에 저장합니다.

여기서 중요한 점은, 중복 체크와 콘텐츠 유효성 검사를 한다는 겁니다. 광고나 쓸모없는 내용은 걸러냅니다.

또한 Daum News는 EUC-KR 인코딩을 사용하는데, 이걸 자동으로 UTF-8로 변환합니다.

**(잠깐 멈춤)**

**2단계는 AI 처리**입니다.

10분마다 스케줄러가 돌면서 네 가지 작업을 순차적으로 실행합니다.

0분에 카테고리 분류, 1분 후에 AI 요약 생성, 2분 후에 키워드 추출, 3분 후에 편향 분석을 합니다.

이렇게 시간차를 두는 이유는 AI 서버에 부하가 한꺼번에 몰리는 걸 방지하기 위해서입니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 7: 신뢰성

그럼 이 뉴스들이 얼마나 신뢰할 수 있는지 설명드리겠습니다.

**(체크 표시를 가리키며)**

**첫째**, 공식 뉴스 포털인 Daum News에서만 수집합니다. 검증되지 않은 블로그나 커뮤니티 글은 수집하지 않습니다.

**둘째**, URL 기반으로 중복을 방지합니다. 같은 기사가 여러 번 저장되는 일이 없습니다.

**셋째**, 콘텐츠 검증을 합니다. 최소 50자 이상, 한글 비율 30% 이상인 기사만 저장합니다. 광고나 UI 요소는 걸러집니다.

**넷째**, 크롤링할 때 요청 간격을 1~2초씩 두어서 상대 서버에 무리를 주지 않습니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 8: AI 서비스

이제 AI 서비스를 자세히 설명드리겠습니다.

**첫 번째, 요약 AI**입니다.

8000번 포트에서 동작하고, T5 한국어 특화 모델을 사용합니다.

기사 본문을 입력하면 100자로 요약해서 출력합니다.

성능은 기사 하나당 1~2초 정도 걸립니다.

**(잠깐 멈춤)**

**두 번째, 편향 분석 AI**입니다.

8002번 포트에서 동작하고, KoBERT 모델을 사용합니다.

세 가지를 분석합니다. 감성 분석으로 긍정, 부정, 중립을 판단하고, 정치 성향으로 진보, 보수, 중도를 분석하고, 키워드 상위 10개를 추출합니다.

**(잠깐 멈춤)**

**세 번째, 카테고리 분류 AI**입니다.

5000번 포트에서 동작하고, Random Forest 머신러닝 알고리즘을 사용합니다.

정확도는 85~90% 정도이고, 8개 카테고리로 자동 분류합니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 9: 주요 기능

사용자 입장에서의 주요 기능을 설명드리겠습니다.

**인증 시스템**부터 보겠습니다.

일반 로그인은 이메일과 비밀번호로 가능하고,

소셜 로그인은 카카오와 네이버를 지원합니다.

특히 계정 연동 기능이 있어서, 일반 계정으로 가입했다가 나중에 카카오 계정을 연동할 수도 있고, 그 반대도 가능합니다.

JWT 토큰의 유효기간은 일반적으로 1일이고, 자동 로그인 체크하면 30일입니다.

**(잠깐 멈춤)**

**뉴스 기능**입니다.

실시간 피드가 있고, 페이지네이션으로 스크롤하면서 볼 수 있습니다.

카테고리별로 조회할 수 있고,

제목이나 내용으로 검색도 가능하고,

인기 뉴스 순위도 제공합니다.

**(잠깐 멈춤)**

**개인화 기능**도 있습니다.

사용자의 읽기 기록을 기반으로 맞춤 추천을 해주고,

북마크 기능,

조회 기록 저장,

댓글 작성 기능이 있습니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 10: API 명세

API는 총 2,694줄로 구성되어 있습니다.

주요 엔드포인트를 보면,

**뉴스 관련**으로는 피드 조회, 상세 조회, 검색 API가 있고,

**인증 관련**으로는 회원가입, 로그인, 카카오 로그인, 네이버 로그인 API가 있습니다.

**추천 시스템**도 있어서 개인화된 뉴스를 추천받을 수 있고,

**댓글 기능**도 완전히 구현되어 있습니다.

모든 API는 RESTful 규칙을 따릅니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 11: 데이터베이스 구조 ⭐⭐⭐

이제 데이터베이스 구조를 자세히 설명드리겠습니다.

**(강조)**

현재 시스템에는 **총 21개의 테이블**이 있습니다.

**(잠깐 멈춤)**

### 핵심 테이블 설명

**첫째, users 테이블**입니다.

**(화면의 사용자 테이블 데이터를 가리키며)**

여기 보시면 실제 데이터가 있는데요,

username, email, password_hash, provider 등의 필드가 있습니다.

특히 주목할 점은 **password_hash 컬럼**입니다.

일반 로그인 사용자는 bcrypt로 암호화된 해시값이 저장되지만,

소셜 로그인 사용자는 NULL로 저장됩니다.

provider 컬럼을 보시면 'local', 'kakao', 'naver' 세 가지 값이 있는데요,

소셜 계정을 연동하면 'local,kakao' 이런 식으로 저장됩니다.

현재 8명의 사용자가 있고, 일반 로그인 3명, 카카오 3명, 네이버 2명입니다.

**(잠깐 멈춤)**

**둘째, news_articles 테이블**입니다.

AI 처리가 완료된 최종 뉴스를 저장합니다.

현재 **239개의 기사**가 저장되어 있고,

제목, 본문, AI 요약, 이미지 URL, 언론사, 카테고리, 기자명, 발행일이 포함됩니다.

카테고리별 분포를 보면, 정치 62개, 경제 57개, 세계 52개, 사회 33개, IT 21개, 생활문화 15개입니다.

**(잠깐 멈춤)**

**셋째, raw_news_articles 테이블**입니다.

크롤링한 원본 뉴스를 임시로 저장하는 테이블입니다.

현재 **412개**가 대기 중이고, AI 처리를 기다리고 있습니다.

processed라는 boolean 컬럼으로 처리 여부를 관리합니다.

**(잠깐 멈춤)**

**넷째, bias_analysis 테이블**입니다.

**(중요하게)**

이게 저희 프로젝트의 핵심인데요,

각 기사마다 편향 분석 결과를 JSON 형태로 저장합니다.

bias_score는 편향 점수를 0.00에서 1.00 사이로 나타내고,

political_leaning은 '진보', '보수', '중립' 중 하나가 저장되고,

analysis_data는 JSONB 타입으로 키워드, 감성 분석, 정당별 언급 횟수 등 상세 분석 결과가 들어갑니다.

confidence는 AI의 확신도를 나타냅니다.

**(잠깐 멈춤)**

**다섯째, user_actions 테이블**입니다.

사용자가 어떤 기사를 봤는지, 북마크했는지, 공유했는지 모든 행동을 기록합니다.

이 데이터를 바탕으로 개인화 추천 알고리즘이 작동합니다.

action_type에는 'VIEW', 'BOOKMARK', 'SHARE' 같은 값이 들어갑니다.

**(잠깐 멈춤)**

**여섯째, keywords 테이블**입니다.

KoNLPy로 추출한 키워드를 저장하고, 빈도수를 관리합니다.

같은 키워드가 여러 기사에서 나오면 frequency 값이 증가합니다.

**(잠깐 멈춤)**

**일곱째, comments 테이블**입니다.

사용자 댓글을 저장하고, 대댓글도 지원합니다.

parent_id 컬럼으로 계층 구조를 만들 수 있습니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 12: 데이터베이스 보안 ⭐⭐⭐

데이터베이스의 보안성에 대해 말씀드리겠습니다.

**(강조)**

**첫째, 비밀번호 암호화**입니다.

사용자 비밀번호는 절대 평문으로 저장하지 않습니다.

bcrypt 알고리즘을 사용해서 해시화하는데, **rounds를 12로 설정**했습니다.

이렇게 하면 무차별 대입 공격에도 안전합니다.

같은 비밀번호라도 매번 다른 해시값이 생성되는 salt가 자동으로 포함됩니다.

**(잠깐 멈춤)**

**둘째, SQL Injection 방지**입니다.

TypeORM을 사용해서 모든 쿼리가 파라미터화됩니다.

사용자 입력값이 SQL 문자열에 직접 들어가지 않고, 바인딩 파라미터로 처리됩니다.

예를 들어 `SELECT * FROM users WHERE username = $1` 이런 식으로 처리돼서,

악의적인 SQL 문이 실행될 수 없습니다.

**(잠깐 멈춤)**

**셋째, 민감 정보 NULL 처리**입니다.

소셜 로그인 사용자는 비밀번호가 필요 없으니까 NULL로 저장합니다.

불필요한 데이터를 저장하지 않는 것도 보안의 일부입니다.

**(잠깐 멈춤)**

**넷째, 외래키 제약 조건**입니다.

모든 관계형 데이터는 외래키로 연결되어 있고, ON DELETE CASCADE 옵션을 사용합니다.

예를 들어 사용자가 탈퇴하면, 그 사용자의 댓글, 북마크, 행동 기록이 자동으로 삭제됩니다.

고아 데이터가 남지 않아서 데이터 무결성이 보장됩니다.

**(잠깐 멈춤)**

**다섯째, 인덱스 최적화**입니다.

자주 조회되는 컬럼에는 인덱스를 걸어서 성능을 향상시켰습니다.

예를 들어 users 테이블의 username과 email에는 UNIQUE 인덱스가 있고,

bias_analysis의 article_id에는 B-tree 인덱스가 있습니다.

**(잠깐 멈춤)**

**여섯째, 환경변수 분리**입니다.

데이터베이스 접속 정보는 절대 코드에 하드코딩하지 않고,

.env 파일로 분리해서 관리합니다.

DB_HOST, DB_PORT, DB_USERNAME, DB_PASSWORD 모두 환경변수로 관리되고,

Git에는 절대 커밋되지 않습니다.

**(잠깐 멈춤)**

**일곱째, 연결 풀 관리**입니다.

TypeORM의 커넥션 풀을 사용해서 데이터베이스 연결을 효율적으로 관리합니다.

동시 접속이 많아도 안정적으로 처리할 수 있습니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 13: 실제 데이터 현황 📊

현재 시스템에 저장된 실제 데이터를 보여드리겠습니다.

**(화면을 가리키며)**

### 사용자 데이터
- **총 사용자**: 8명
- **일반 로그인**: 3명 (admin, hong_gildong, kim_user)
- **카카오 로그인**: 3명 (password_hash = NULL)
- **네이버 로그인**: 2명 (password_hash = NULL)

보시면 소셜 로그인 사용자는 비밀번호가 NULL로 표시되고,

일반 로그인 사용자는 '암호화됨'으로 표시됩니다.

**(잠깐 멈춤)**

### 뉴스 데이터
- **처리 완료 기사**: 239개 (news_articles)
- **처리 대기 기사**: 412개 (raw_news_articles)
- **총 수집량**: 651개

카테고리별로 보면 정치가 62개로 가장 많고, 경제 57개, 세계 52개 순입니다.

**(잠깐 멈춤)**

### 편향 분석 데이터
실제 분석 결과를 보시면,

한 기사의 bias_score는 0.00으로 중립이고,

political_leaning은 'neutral'로 분류되었습니다.

confidence는 0.95로 95% 확신도를 나타냅니다.

analysis_data를 보시면 JSON 형태로 키워드, 감성 분석, 정당별 언급 횟수가 모두 저장되어 있습니다.

**(잠깐 멈춤)**

### 사용자 행동 데이터
현재 1명의 사용자(poungkoo)가 1개의 기사를 VIEW(조회)했습니다.

이런 데이터가 쌓이면 개인화 추천 알고리즘이 작동합니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 14: 배포 환경

배포는 Docker Compose로 구성했습니다.

**(강조)**

총 **12개의 컨테이너**가 동작합니다.

PostgreSQL 데이터베이스,

Redis 캐시,

Node.js로 만든 메인 API,

React 프론트엔드,

뉴스를 수집하는 API 크롤러,

병렬 처리를 위한 Puppeteer 크롤러 3개,

10분마다 작업을 실행하는 스케줄러,

요약 AI,

편향 분석 AI,

분류 AI

이렇게 12개입니다.

**(잠깐 멈춤)**

포트 구성을 보면,

3000번이 메인 API,

3001번이 프론트엔드,

5000, 8000, 8002번이 AI 서비스,

5432번이 PostgreSQL,

6379번이 Redis입니다.

Docker Compose 파일 하나로 전체 시스템을 한 번에 실행할 수 있습니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 15: 성능 지표

성능 지표를 말씀드리겠습니다.

**처리량**은 일일 19,200개의 기사를 수집하고,

AI 처리는 10분마다 배치로 실행되며,

API 응답 시간은 평균 200밀리초 이하입니다.

**(잠깐 멈춤)**

**확장성** 측면에서는,

설계상 동시 접속 500명 이상을 지원하고,

AI 서버를 수평 확장할 수 있어서 부하가 늘어나면 서버를 추가할 수 있습니다.

Redis 캐싱을 활용해서 자주 조회되는 데이터는 캐시에서 빠르게 응답합니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 16: 개발 특이사항

개발 중 중요한 의사결정이 있었습니다.

처음에는 Spark, Kafka, Airflow를 사용한 빅데이터 아키텍처로 설계했습니다.

하지만 개발 중에 문제점을 발견했습니다. 메모리 사용량이 너무 많고, 배포가 복잡하고, 유지보수가 어려웠습니다.

**(강조)**

그래서 아키텍처를 변경했습니다.

Node.js, Python, 그리고 간단한 Cron 스케줄러로 대체했습니다.

결과적으로 **메모리 사용량이 75% 감소**했고, 배포도 훨씬 간단해졌습니다.

**(잠깐 멈춤)**

기술 선택 이유를 말씀드리면,

TypeScript는 타입 안전성 때문에 선택했고,

FastAPI는 AI 서비스의 고속 처리를 위해 선택했으며,

PostgreSQL은 JSONB를 지원해서 복잡한 데이터 구조를 저장할 수 있기 때문에 선택했고,

Docker는 배포의 편의성 때문에 선택했습니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 17: 향후 계획

향후 계획을 말씀드리겠습니다.

**단기적으로는**,

Elasticsearch를 도입해서 검색 성능을 개선하고,

Redis 캐싱을 더 확대하고,

프론트엔드에 React Query를 도입해서 최적화할 계획입니다.

**중기적으로는**,

WebSocket을 사용한 실시간 알림 기능을 추가하고,

모바일 앱을 개발하고,

AI 추천 알고리즘을 더 고도화할 계획입니다.

**장기적으로는**,

Kubernetes로 배포해서 클라우드 환경에서 자동 확장되도록 하고,

CI/CD 파이프라인을 구축해서 자동 배포를 구현하고,

영어, 중국어 등 다국어를 지원할 계획입니다.

**(다음 슬라이드로)**

---

## 🎤 슬라이드 18: 핵심 강점 (마무리)

마지막으로 FANS의 핵심 강점을 정리하겠습니다.

**(체크 표시를 가리키며)**

**첫째, 완전한 자동화**입니다.

3분마다 자동 크롤링, 10분마다 AI 처리, 수동 개입이 거의 필요 없습니다.

**둘째, 높은 신뢰성**입니다.

공식 뉴스 포털에서만 수집하고, 중복을 방지하며, 콘텐츠를 철저히 검증합니다.

**셋째, 강력한 AI 활용**입니다.

한국어에 특화된 T5와 KoBERT 모델을 사용하고,

분류, 요약, 편향 분석까지 3단계 AI 처리를 합니다.

**넷째, 우수한 사용자 경험**입니다.

카카오와 네이버 소셜 로그인을 지원하고,

개인화 추천 기능이 있으며,

직관적인 UI를 제공합니다.

**다섯째, 강력한 데이터 보안**입니다.

bcrypt 암호화, SQL Injection 방지, 외래키 제약, 환경변수 분리로 데이터를 안전하게 보호합니다.

**(잠깐 멈춤)**

이상으로 FANS 뉴스 큐레이션 플랫폼 발표를 마치겠습니다.

**(자신 있게)**

개발 기간은 2025년 9월부터 10월까지 약 2개월이었고,

React, Node.js, Python, PostgreSQL을 주요 기술로 사용했으며,

Docker Compose로 12개의 컨테이너를 관리하고,

21개의 데이터베이스 테이블로 651개의 뉴스를 관리하고 있습니다.

질문 있으시면 답변드리겠습니다. 감사합니다.

---

# 📊 캡쳐용 데이터베이스 샘플 데이터

## 1️⃣ Users 테이블 (사용자)

```
ID | Username     | Email                  | Password         | Name   | Phone         | Provider | Active | Created
----|--------------|------------------------|------------------|--------|---------------|----------|--------|------------------
6  | socialtest1  | social@test.com        | NULL (소셜)      | 테스트 | 01012345678   | kakao    | ✓      | 2025-10-21 10:27
7  | chloe        | kakao_445@kakao.local  | NULL (소셜)      | :)     | 01000000000   | kakao    | ✓      | 2025-10-21 10:28
8  | poungkoo     | poungkoo@naver.com     | NULL (소셜)      | 김현애 |               | naver    | ✓      | 2025-10-21 10:34
9  | admin        | admin@fans.com         | 암호화됨         | 관리자 | 010-1234-5678 | local    | ✓      | 2025-10-21 10:58
10 | hong_gildong | hong@example.com       | 암호화됨         | 홍길동 | 010-2345-6789 | local    | ✓      | 2025-10-16 10:58
11 | kim_user     | kim@example.com        | 암호화됨         | 김철수 | 010-3456-7890 | local    | ✓      | 2025-10-18 10:58
12 | lee_kakao    | lee@kakao.com          | NULL (소셜)      | 이영희 | 010-4567-8901 | kakao    | ✓      | 2025-10-19 10:58
13 | park_naver   | park@naver.com         | NULL (소셜)      | 박민수 | 010-5678-9012 | naver    | ✓      | 2025-10-20 10:58
```

### 특징
- ✅ **일반 로그인**: bcrypt 해시값 저장
- ✅ **소셜 로그인**: password_hash NULL
- ✅ **계정 연동**: provider에 'local,kakao' 형태

---

## 2️⃣ News Articles 테이블 (뉴스 기사)

```
ID  | Title                                          | Category | Source   | Date
----|------------------------------------------------|----------|----------|------------
344 | "요즘 은행 안보이더니…" 지점 3곳 중 1곳이...  | 정치     | 문화일보 | 2025-10-21
343 | "외출" 말하자 조명 끄고 청소…삼성vsLG...      | 경제     | 한겨레   | 2025-10-21
342 | 외부인이 들어오자 마을이 활기를...            | 세계     | 동아일보 | 2025-10-21
341 | "주인님을 위한 '사랑의 뺨 맞기'"...          | 사회     | 조선일보 | 2025-10-21
335 | 캄보디아서 군사작전? '아세안 데뷔'...        | 정치     | 한겨레   | 2025-10-21
```

### 통계
- **총 기사**: 239개 (처리 완료)
- **대기 중**: 412개 (raw_news_articles)
- **카테고리 분포**: 정치 62, 경제 57, 세계 52, 사회 33, IT 21, 생활문화 15

---

## 3️⃣ Bias Analysis 테이블 (편향 분석)

```json
{
  "article_id": 1,
  "bias_score": 0.00,
  "political_leaning": "중립",
  "confidence": 0.95,
  "analysis_data": {
    "sentiment": {
      "sentiment": "negative",
      "confidence": 0.95,
      "score": -1,
      "positive_count": 0,
      "negative_count": 1
    },
    "keywords": [
      {"word": "라고", "score": 0.28},
      {"word": "지사는", "score": 0.28},
      {"word": "공흥지구", "score": 0.22}
    ],
    "political": {
      "stance": "중도",
      "party_analysis": {
        "여당": {"count": 2, "positive": 0, "negative": 0},
        "야당": {"count": 3, "positive": 0, "negative": 0}
      }
    }
  }
}
```

---

## 4️⃣ Categories 테이블 (카테고리별 분포)

```
Category   | Count
-----------|-------
정치       | 62
경제       | 57
세계       | 52
사회       | 33
IT/과학    | 21
생활/문화  | 15
```

---

## 5️⃣ User Actions 테이블 (사용자 행동)

```
User ID | Username | Action Type | Count
--------|----------|-------------|-------
8       | poungkoo | VIEW        | 1
```

### 행동 유형
- **VIEW**: 기사 조회
- **BOOKMARK**: 북마크
- **SHARE**: 공유

---

# 📌 발표 팁

## ⏱️ 시간 배분 (총 12~18분 기준)
- 슬라이드 1-2: 도입 (1분)
- 슬라이드 3-4: 기술 스택 및 아키텍처 (2분)
- 슬라이드 5-7: **크롤링 시스템** ⭐ (3분)
- 슬라이드 8: **AI 서비스** ⭐ (2분)
- 슬라이드 9-10: 기능 및 API (2분)
- 슬라이드 11-13: **데이터베이스** ⭐⭐⭐ 핵심 (5분)
- 슬라이드 14-17: 배포 및 계획 (2분)
- 슬라이드 18: 마무리 (1분)

## 🎯 강조할 포인트
1. **하루 19,200개** 자동 수집
2. **21개 테이블** 구조
3. **bcrypt 암호화** (rounds=12)
4. **JSONB 편향 분석** 데이터
5. **651개 뉴스** 현재 보유
6. **SQL Injection 방지**

## 💡 데이터베이스 관련 예상 질문 답변

### Q1: "왜 PostgreSQL을 선택했나요?"
**A**: "PostgreSQL은 JSONB 타입을 지원해서 편향 분석 같은 복잡한 데이터를 효율적으로 저장할 수 있습니다. 또한 외래키 제약과 트랜잭션이 안정적이고, TypeORM과의 호환성도 좋습니다."

### Q2: "소셜 로그인 사용자가 비밀번호를 나중에 설정할 수 있나요?"
**A**: "네, 가능합니다. 소셜 로그인 후 마이페이지에서 비밀번호를 설정할 수 있고, 설정하면 일반 로그인과 소셜 로그인 둘 다 사용할 수 있습니다. provider 컬럼이 'kakao'에서 'local,kakao'로 변경됩니다."

### Q3: "데이터베이스 백업은 어떻게 하나요?"
**A**: "PostgreSQL의 pg_dump를 사용해서 정기적으로 백업할 계획입니다. 또한 Docker Volume으로 데이터를 영구 저장하고 있어서, 컨테이너가 재시작되어도 데이터는 유지됩니다."

### Q4: "JSONB로 저장하면 쿼리가 느리지 않나요?"
**A**: "PostgreSQL의 JSONB는 바이너리 형태로 저장돼서 일반 JSON보다 빠르고, GIN 인덱스를 걸면 내부 필드도 빠르게 검색할 수 있습니다. 현재는 데이터량이 많지 않아서 성능 문제는 없습니다."

### Q5: "사용자가 탈퇴하면 데이터는 어떻게 되나요?"
**A**: "외래키에 ON DELETE CASCADE 옵션을 설정해서, 사용자가 탈퇴하면 그 사용자의 댓글, 북마크, 행동 기록이 자동으로 삭제됩니다. 다만 뉴스 기사 자체는 삭제되지 않습니다."

### Q6: "테이블이 21개나 되는데 복잡하지 않나요?"
**A**: "각 테이블이 명확한 역할을 가지고 있어서 오히려 관리가 쉽습니다. 정규화를 통해 데이터 중복을 최소화했고, TypeORM으로 관계를 자동으로 처리해서 개발도 편리합니다."

## 🎬 발표 시작 & 마무리 멘트

### 시작
"안녕하세요. FANS 프로젝트를 발표할 [이름]입니다. 오늘은 AI를 활용한 뉴스 큐레이션 플랫폼, FANS를 소개하겠습니다."

### 마무리 (업데이트)
"이상으로 FANS 발표를 마치겠습니다. 자동화된 뉴스 수집, AI 기반 분석, 그리고 21개 테이블로 구성된 안전한 데이터베이스까지, 신뢰할 수 있는 뉴스 플랫폼을 만들기 위해 노력했습니다. 현재 651개의 뉴스가 저장되어 있고, 매일 19,200개씩 자동으로 수집되고 있습니다. 질문 있으시면 답변드리겠습니다. 감사합니다."
